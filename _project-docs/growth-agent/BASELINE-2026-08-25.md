# Baseline — measured before any homepage change

**Captured:** 2026-08-25, before the rework in `homepage-rework.html` was applied.
Re-run the same checks after the change and diff against this file. Do not edit these
numbers — add an "after" column in a new file instead.

## Homepage, what a crawler receives

| Metric | oBizee | SitesPlaced | Dukaan | Bikayi |
|---|---|---|---|---|
| Visible text (chars) | **93** | **13581** | 6,871 | 97 |
| `<h1>` on homepage | 0 | 1 | — | — |
| Images | 0 | 110 | — | — |
| Links | 0 | many | — | — |
| Web fonts loaded | **none** | Barlow + Inter | Archivo + Libre | none |

oBizee's homepage renders entirely client-side, so the server response carries the
title tag and nothing else.

## dm2buy comparison pages

| Metric | oBizee `/compare/best-dm2buy-alternatives/` | SitesPlaced `/dm2buy-alternative` |
|---|---|---|
| Word count | ~1,100 | ~2,475 |
| Tables | 0 | 0 |
| Schema markup | FAQPage + ItemList | **none** |
| Named customers | 0 | 8 on page, 26 site-wide |
| Migration offer | no | yes, 24 hours |
| Prices stated | partial | ₹0 / ₹499 / competitor ₹999 |

They rank with no schema and no tables. Depth, named proof and a concrete offer are
doing the work.

## Site claims vs reality

| Claim on site | Actual | Gap |
|---|---|---|
| 35+ active merchants | 165 | understated 4.7x |
| 40L+ revenue processed | 3,281 orders | orders never mentioned |
| — | 887 products, top 4 sellers | never mentioned |

## Known technical defects at baseline

- Homepage client-rendered — 93 chars to crawlers
- Sitemap index `lastmod` for `sitemap-compare.xml` stale at 2026-04-22; pages edited 2026-08-20
- All 12 compare URLs in sitemap lack the trailing slash the site canonicalises to — every one 301s
- `obizee.com` deep paths 404; only the apex root redirects to www
- `template-bold-v1` storefront template emits no structured data
- No product feed endpoint anywhere — nothing reaches Merchant Center, Meta or AI crawlers

## Search Console at baseline

- 244 pages not indexed, 44 of them Soft 404 (all merchant storefronts)
- Storefront `/products` SSR fixed 2026-08-25 (`fd8c0a2`) — recrawl not yet reflected

## How to re-measure

Run the identical extraction so the comparison is honest: strip `<script>` and
`<style>`, strip tags, collapse whitespace, count characters. Anything else is not
the same measurement.
